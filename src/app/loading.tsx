import TedxSpinner from "@/components/ui/TedxSpinner";

export default function Loading() {
  return (
    <div className="min-h-[70vh] flex items-center justify-center">
      <TedxSpinner />
    </div>
  );
}
