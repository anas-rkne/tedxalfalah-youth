import { notFound } from "next/navigation";

// هذه الصفحة مخفية حالياً بناءً على طلب العميل. يمكن إعادتها لاحقاً.
// لاسترجاعها: أعد المحتوى السابق أو أزل هذا الملف

export default function HiddenSpeakersPage() {
  notFound();
}