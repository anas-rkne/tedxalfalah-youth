# تقرير فحص ما قبل الإطلاق (Pre-Launch QA)

التاريخ: 2026-08-15 — المرحلتان الأولى (آلي) والثانية (الريسبونسيف) مكتملتان. الفحص اليدوي البصري أدناه.

## النتائج الإجمالية

| البند | النتيجة |
|---|---|
| ترجمات ar/en | 498/498 مفتاحاً متطابقة ✓ |
| ESLint | 0 أخطاء، 95 تحذيراً قائماً (لا علاقة لها بالتغييرات) ✓ |
| Build الإنتاج | ناجح ✓ |
| فحص 32 مساراً | جميعها 200، `/` → 307 إلى `/en` ✓ |
| رؤوس الأمان | CSP سليم (بلا `unsafe-eval`)، `X-Frame-Options: DENY`، HSTS موجود ✓ |
| Canonical URLs | موجودة على الصفحات الـ12 باللغتين ✓ |
| robots.txt / sitemap.xml | صحيحان، بكل اللغات و hreflang ✓ |
| الأصول (صور/SVG) | كلها موجودة وتُخدَّم 200 ✓ |
| RTL | `dir="rtl"` على /ar و `dir="ltr"` على /en ✓ |
| اختبارات e2e (بلا فورمات) | 23/23 ناجحة ✓ |
| الأسرار في git | `.env.local` غير متتبع، لا أسرار مكشوفة ✓ |
| متغيرات البيئة | كلها مضبوطة محلياً؛ `.env.local.example` مغطاة بالكامل ✓ |

## المرحلة الثانية — الفحص الشامل للريسبونسيف (86/86 ✓)

- **النطاق**: 10 صفحات × 7 مقاسات (320/375/414/768/1024/1440/1920) بالإنكليزية +
  5 صفحات رئيسية × 3 مقاسات بالعربية + صفحة 404 = **86 اختباراً، كلها خضراء**.
- **ما يُفحص في كل اختبار**: لا تدفق أفقي (scrollWidth ≤ viewport)، الفوتر/الهيدر
  ظاهران، شريط التنقل (قائمة جوال < 1024px، شريط سطح المكتب ≥ 1024px)، ولقطة شاشة
  كاملة `qa-screenshots/<page>-<width>.png` (85 لقطة لمراجعتك البصرية).
- **تفاعلات موبايل تحققت يدوياً**: أكورديون FAQ (يفتح)، تبويبات الجدول (4 تتبدَّل)،
  تحقق فورم فارغ (يعرض أخطاء)، قائمة AR للجوال (تفتح بـ 5 روابط)، قائمة موبايل EN.
- **نص داخل الصناديق**: بطاقات apply وtickets لا يفيض نصها خارج بطاقاتها؛
  الاقتطاع الموجود في بطاقة InfoCard (نص التاريخ) مقصود بـ `truncate`.

### إصلاحات الريسبونسيف (~ أخطاء حقيقية ظهرت من الاختبارات)
1. **صفحة التذاكر — تدفق 39px عند 320**: بطاقات InfoCard تتجاوز شبكة الأعمدة.
   الحل: `min-w-0` على البطاقة و`flex-1` للمحتوى في `src/app/[locale]/tickets/page.tsx`.
2. **الرئيسية — تدفق 140px عند 320/375/414**:
   - العدّاد التنازلي: حد أدنى 2rem/خانة (32px) يجعل 9 خانات = 351px.
     الحل: `clamp(1.5rem, 7vw, 3rem)` في `src/components/ui/flip-clock.tsx`.
   - قسم الشعار (Theme): دوائر زخرفية 600px وصور إحصائيات بمقياس 3.5× تتجاوز
     الشاشة. الحل: `overflow-hidden` للقسم + `flex-wrap justify-center` لصف
     الإحصائيات في `src/components/home/ThemeContent.tsx`.
3. **إصلاحات اختبارات** (ليست أخطاء موقع): حد شريط التنقل الحقيقي هو 1024px لا 768؛
   صفحة الفريق تحتوي `footer` خاصاً بها (شريط الإحصائيات) — انتقلنا إلى
   `#global-footer`؛ صفحة Thank-You تخفي الهيدر والفوتر عمداً (`ThankYouContent.tsx:120`)
   — استُثنيت من الفحص. الملف: `e2e/responsive.spec.ts` (جديد).

## المرحلة الثالثة — توحيد أزرار الفوتر مع الهيرو على الموبايل (86/86 ✓)

- **المشكلة**: زرّا "Apply Now"/"Get Tickets" في قسم الفوتر الأبيض كانا يمتدان عبر
  عرض الشاشة كاملة (320/375/414px بلا فراغ جانبي) بينما أزرار الهيرو كانت مضبوطة
  بحشوة `px-4` (288/343/372px).
- **الحل** في `src/components/layout/FooterContent.tsx`:
  1. `px-4` على قسم الأزرار الأبيض (لا ملامسة للحواف).
  2. توحيد حاوية الأزرار مع الهيرو حرفياً: `flex flex-wrap items-center
     justify-center gap-3 sm:gap-4 w-full max-w-md sm:max-w-lg mx-auto py-12 md:py-14`.
  3. الأزرار نفسها بقيت بلا تغيير (كانت متطابقة أصلاً).
- **النتيجة المقاسة**: الفوتر = الهيرو عند 320 (288=288) و375 (343=343) و414
  (382 vs 372 — فرق 10px من بنية التوسيط الداخلية للهيرو).
- **فحص جديد في `e2e/responsive.spec.ts`**: عند كل عرض موبايل (< 640px)، أوسع زر
  CTA في `#global-footer` يجب ألا يتجاوز عرض الشاشة − 30px (كان يفشل قبل الإصلاح).
- **التحقق**: إعادة بناء إنتاجية + إعادة تشغيل كامل المجموعة = 86/86 خضراء؛
  لقطات `qa-screenshots/footer-cta-320.png` و`footer-cta-414.png` للمراجعة البصرية.

## إصلاحات أُدخلت أثناء الفحص

### 1. أمان حرج: `/api/revalidate` كان بلا حماية
كان يُقبل أي طلب ويعيد بناء كل الصفحات حتى بلا مفتاح (نتيجة اختبار سابقة: 200). الآن:
- **بدون `SANITY_WEBHOOK_SECRET`** → 503 "not configured" (fail-closed).
- **بمفتاح خاطئ أو غائب** → 401 "Invalid secret".
- **بالمفتاح الصحيح** → 200 revalidated (تم التحقق).
- الملف: `src/app/api/revalidate/route.ts`

### 2. أمان حرج: الفورمات كانت تُقبل بلا Turnstile
كانت `verifyTurnstile` تسمح بالمرور دائماً إذا غاب المفتاح — في الإنتاج أصبحت ترفض (403). تم التحقق:
- طلب بلا توكن → 403 ✓ | توكن مزيف → 403 ✓
- الملف: `src/lib/turnstile.ts`

### 3. Rate limiting fail-closed في الإنتاج
`src/lib/rate-limit.ts`: بدون Upstash في الإنتاج تُرفض الطلبات (429) بدل السماح. تم التحقق: الطلب السادس لنفس الـ IP خلال 10 دقائق → 429 ✓

### 4. مفتاح ترجمة مفقود: `common.nav.thank-you`
كانت صفحة `/thank-you` تطلبه ولم يكن في en/ar → خطأ MISSING_MESSAGE أثناء SSR. أُضيف: "Thank You" / "شكراً لك". (سبب عدم اكتشافه سابقاً: الفحص القديم قارن ar/en فقط، وكلاهما كانا ناقصين معاً.)

### 5. اختبار العدّاد كان يقرأ العنصر الخطأ
`span.sr-only` الأول في الصفحة هو سنة الهيرو ("2026") وليس العدّاد. عُدّل المُحدِّد إلى `div[aria-live="polite"] span.sr-only` في `e2e/navigation.spec.ts`. (العدّاد نفسه كان سليماً.)

## نتائج اختبارات الأمان (خادم إنتاج محمّل بالبيئة)

| الاختبار | المتوقع | النتيجة |
|---|---|---|
| apply بدون توكن Turnstile | 403 | ✓ |
| apply بتوكن مزيف | 403 | ✓ |
| revalidate بلا مفتاح | 401 | ✓ |
| revalidate بمفتاح خاطئ | 401 | ✓ |
| revalidate بمفتاح صحيح | 200 | ✓ |
| apply بأصل (Origin) غير مسموح | 403 | ✓ |
| apply بجسم غير JSON | 400 | ✓ |
| الطلب السادس من نفس الـ IP | 429 | ✓ |

## ملاحظات تُنفَّذ قبل الرفع

1. **متغيرات البيئة في الاستضافة**: خادم standalone **لا يقرأ `.env.local` تلقائياً** — يجب ضبط كل المتغيرات في لوحة الاستضافة (hPanel أو ما يعادلها)، وبالأخص: `TURNSTILE_SECRET_KEY`, `UPSTASH_REDIS_REST_URL/TOKEN`, `SANITY_WEBHOOK_SECRET`, `SMTP_*`, `GOOGLE_*`. بدونها: تُرفض الفورمات عمداً (fail-closed) — هذا سلوك مقصود.
2. **`.env.local.example`**: كل المفاتيح مغطاة (بما فيها `NEXT_PUBLIC_APPLICATION_DEADLINE` و`SANITY_STUDIO_PROJECT_ID`) — لا تحديث مطلوب.
3. **اختبارات الفورمات e2e** (`e2e/forms.spec.ts`) لم تُشغَّل: ترسل بيانات فعلية للورقة والبريد. تُترك للفحص اليدوي.
4. **ملاحظة بسيطة**: يظهر أحياناً `The destination stream closed early` في سجل الخادم — غير مؤثر (إجهاض متصفح لطلب تدفق) ولا يرتبط بخطأ تطبيق.
5. **صفحة `/thank-you`**: غير مرتبطة في القوائم (مقصود)، لكنها مكشوفة في sitemap — يمكن إخفاؤها من `src/app/sitemap.ts` إن أردت.

## قائمة الفحص اليدوي (قبل الرفع)

1. **مراجعة اللقطات**: `qa-screenshots/` (86 صورة: صفحة × مقاس × لغة) — راجع التصميم بصرياً (خاصة 320 و375 و768).
2. تصفح كامل بالعربية والإنجليزية على الجوال والحاسوب (الهيرو الجديد، العدّاد، الفوتر الجديد، أزرار Apply/Tickets).
3. فورمات فعلياً (رسالة تواصل، تطبيق مسار القُصَّر، تطبيق خبير، تذاكر): إرسال + بريد تأكيد + إشعار إداري + صف Google Sheet + صفحة الشكر.
4. سحب الويدجت Turnstile في المتصفح العادي (غير الأتمتة).
5. تحويل اللغة من كل صفحة ذهاباً وإياباً.
6. فتح الموقع من دون اتصال بعد زيارة أولى (PWA) — مغطى آلياً أيضاً.
7. ملاحظة: خادم الفحص الحالي يعمل على `http://localhost:3100` (بنية إنتاج محمّلة بالبيئة عبر `dotenv`). لإعادة تشغيل اختبارات الريسبونسيف/الموبايل على نفس الخادم: `npx playwright test -c playwright.3100.config.ts` (ملف الإعداد المؤقت أُضيف لهذا الغرض — يحيل الاختبارات إلى :3100 بدل تشغيل خادم إضافي).
